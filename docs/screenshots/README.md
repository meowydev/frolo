# Screenshots

These are **placeholders**. Replace them with real captures before publishing the
repository. The README references `hero.png`.

Suggested captures (mock mode, so no real infrastructure is shown):

- `hero.png` — the Welcome screen with the "Simulated infrastructure (mock)"
  indicator and the tier pill.
- `inventory.png` — templates + VMs inventory.
- `wizard.png` — the deployment wizard (resources / network / recipe).
- `review.png` — the plan / mutation review with the exposure hop breakdown.
- `timeline.png` — a live deployment timeline showing observed status text
  (e.g. "HTTP 200, marker found").
- `details.png` — deployment details with the local and simulated public
  addresses.

Guidelines:
- Use mock mode; never capture real Proxmox hosts, router UIs, IPs, or
  credentials.
- Keep the window at ~1100×760 to match the app's default size.
